<div class="container top-80">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <h1 class="text-center text-info">无猜</h1>
            <p class="description text-center text-info">这里有一群两小无猜的小伙伴！快来加入我们吧</p>
            {{#if flash}}
            <div class="alert alert-warning alert-dismissible" role="alert">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <p>{{flash.msg}}</p>
            </div>
            {{/if}}
            <form method="POST" action="/avatarSetting" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="avatar">选择文件</label>
                    <input type="file" class="form-control" name="avatar" id="avatar" placeholder="选择你要上传的头像">
                </div>
                <button type="submit" class="btn btn-primary btn-block">设置头像</button>
            </form>
        </div>
    </div>
</div>